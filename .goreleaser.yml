# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2
project_name: dcli

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - id: dcli
    main: ./main.go
    binary: dcli
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/oleg-koval/dcli/cmd.Version={{.Version}}

archives:
  - id: dcli
    ids:
      - dcli
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: '{{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}'
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

release:
  name_template: 'Release {{.Version}}'
  draft: false
  # Mark GitHub prerelease when the tag looks like a pre-release (e.g. v0.2.0-beta.1+sha).
  prerelease: auto
  footer: |
    ## Installation

    ### Homebrew
    ```bash
    brew tap oleg-koval/dcli
    brew install dcli
    ```

    ### Direct Download
    Download the binary for your platform above.

    ### From Source
    ```bash
    git clone https://github.com/oleg-koval/dcli.git
    cd dcli
    make build
    ./bin/dcli --version
    ```

brews:
  - name: dcli
    repository:
      owner: oleg-koval
      name: homebrew-dcli
      branch: main
      token: '{{ .Env.HOMEBREW_TAP_TOKEN }}'
    commit_author:
      name: dcli-bot
      email: bot@dcli.dev
    commit_msg_template: 'chore(release): update dcli formula to {{ .Version }}'
    homepage: https://github.com/oleg-koval/dcli
    description: Lightweight Docker Compose and Git management CLI
    license: MIT
    directory: Formula
    install: |
      bin.install "dcli"
    test: |
      system "#{bin}/dcli --version"
    dependencies:
      - name: docker
        type: optional
      - name: git
        type: optional

changelog:
  disable: false
  use: git
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore'
      - 'merge pull request'
      - 'Merge pull request'
      - 'Merge branch'
